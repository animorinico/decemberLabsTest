using AventStack.ExtentReports;

namespace DecemberLabsTest.utils
{
    enum StatusTest
    {
        INFO = Status.Info,
        WARN = Status.Warning,
        SKIP = Status.Skip,
        PASS = Status.Pass,
        FAIL = Status.Fail
    }
}
